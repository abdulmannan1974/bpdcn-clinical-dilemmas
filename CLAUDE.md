## Branding
- #BloodDoctor is a MANDATORY hashtag on ALL LinkedIn content

## Behavioral Rules (CRITICAL — applies to Code tab AND Cowork tab)
- Do NOT create todo lists, task lists, or use TodoWrite/TaskCreate tools
- Do NOT use TaskUpdate, TaskGet, or any task-tracking tools
- Do NOT "resume from where we left off" — just do the work
- Do NOT loop on planning — state approach in 1-2 sentences, then EXECUTE immediately
- If a file is too large, read it in chunks sequentially WITHOUT creating a todo list between chunks
- NEVER write "Update todo list" or "Let me set up the TodoWrite"
- When given a task: READ → PROCESS → OUTPUT. No intermediate tracking.

## Skills Routing
- Skills catalogue is in memory: `memory/blood-doctor-skills-catalogue.md`
- Installed skills are in `~/.claude/skills/`
- Plugin skills (.skill/.plugin) are on Google Drive: `10_TOOLS_AND_APPLICATIONS/Claude_Skills/`
- To use a Drive skill: copy to /tmp/, unzip, read SKILL.md, apply framework
